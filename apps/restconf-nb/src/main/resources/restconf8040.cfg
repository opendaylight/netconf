#example of restconf8040 configuration
#Default values:
#pretty-print=false  —Control the default value of the odl-pretty-print query parameter.
#data-missing-is-404=false  —Control the HTTP status code reporting of conditions corresponding to "data-missing".
#                            When this is set to true, the server will violate RFC8040 and report "404" instead of "409".
#maximum-fragment-length=0  —Maximum SSE fragment length in number of Unicode code units (characters)
#heartbeat-interval=10000  —Interval in milliseconds between sending of ping control frames.
#restconf=rests  —The value of RFC8040 {+restconf} URI template, pointing to the root resource. Must not end with '/'.
#ping-executor-name-prefix=ping-executor  —Name of thread group Ping Executor will be run with.
#max-thread-count=1  —Number of threads Ping Executor will be run with.

# Netty endpoint related settings
# Server bind address
#bind-address=0.0.0.0
# Server bind port
#bind-port=8182
# The path to RESTCONF API root, as a RFC3986 'path-rootless' string.
#api-root-path=restconf
# The number of Netty boss threads. 0 means the Netty default, which is usually twice the number of CPU cores.
#boss-threads=0
# The number of Netty worker threads. 0 means the Netty default, which is usually twice the number of CPU cores.
#worker-threads=0
# Maximum number of response bytes buffered before switching to chunk streaming.
#http-chunk-size=262144
# Netty's thread limit. 0 means no limits
#group-threads=0
# Default encoding for outgoing messages. Expected values are 'xml' or 'json' (without quotes).
#default-encoding=json
# Maximum HTTP/2 frame payload size in bytes this server is willing to accept from the client
# (SETTINGS_MAX_FRAME_SIZE advertised by the server). If not overridden, the RFC7540 default 16384 is used.
# Valid range is 16384–16777215.
#http2-max-frame-size=16384
<<<<<<< HEAD   (bd3e0a Refactor pipeline setup)
=======
# Netty write buffer low watermark in bytes. A channel becomes writable again when queued outbound bytes
# fall below this value.
#http-write-buffer-low-watermark=32768
# Netty write buffer high watermark in bytes. A channel becomes unwritable when queued outbound bytes
# exceed this value.
# To prevent OutOfMemoryError (OOM) crashes, ensure that
# is significantly lower than the maximum memory allocated to the JVM (-Xmx and MaxDirectMemorySize).
# Setting this value too high defeats the backpressure mechanism and risks exhausting server RAM.
#http-write-buffer-high-watermark=65536
# HTTP/3 Alt-Svc max-age (seconds). 0 disables Alt-Svc advertisement.
# The advertised port is always bind-port. Alt-Svc is only sent when TLS is enabled.
# Valid range is 0–2147483647.
#http3-alt-svc-max-age=0
# HTTP/3 QUIC transport tuning:
# Initial max connection data limit in bytes.
#http3-initial-max-data=4194304
# Initial max stream data limit in bytes for remotely-initiated bidirectional streams.
# Locally-initiated bidirectional stream limit is fixed to 262144 bytes.
#http3-initial-max-stream-data-bidirectional-remote=262144
# Initial max number of bidirectional streams.
#http3-initial-max-streams-bidirectional=100
>>>>>>> CHANGE (3b9215 Add HTTP write buffer watermarks to RFC8040 config)

# Netty Tls transport configuration.
# Both certificate and private key are required in order to enable.
# X509 certificate file in PEM format
#tls-certificate=etc/tls/cert.pem
# Private key file in PEM format
#tls-private-key=etc/tls/key.pem
