module top {
  namespace "http://example.com/schema/1.2/config";
  prefix test-m;

  container top {
    list interfaces {
      container interface {
        leaf name {
          type string;
        }
      }
    }

    list users {
      container user {
        leaf name {
          type string;
        }
      }
    }
  }
}
